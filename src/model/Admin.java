package model;

public class Admin extends Person {

    Admin(int id, String name, String surname, String dateOfBrith, String position, double salary) {
        super(id, name, surname, dateOfBrith, position, salary);
    }
}
